// 本地配置信息读取
//def localSettingsFile = rootProject.file("localSettings.properties")
//def localSettingsProperties = new Properties()
//localSettingsProperties.load(new FileInputStream(localSettingsFile))
//def singleModule = localSettingsProperties['SINGLE_MODULE']

//根据是否独立运行，将模块作为apk还是module
if (rootProject.singleModule) {
    apply plugin: 'com.android.application'
} else {
    apply plugin: 'com.android.library'
}

apply from: '../base_module.gradle'

android {
    defaultConfig {
        applicationId "pub.gll.onepeas.todo"

        vectorDrawables {
            useSupportLibrary true
        }
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {


}